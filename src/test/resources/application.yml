spring:
# datasource Setting
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  # JPA Setting
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true

# Spring Boot는 DB 정보 설정이 안 되어 있으면 인메모리 버전으로 테스트를 돌리게 된다.

# Logging Setting
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace # query parameter 확인